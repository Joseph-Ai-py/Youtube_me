# YouTube Me — 채택 기능 구현 계획

현재 구현과 검토 문서의 대조 결과, 다음 기능을 우선 채택한다.

1. 데이터 기간과 완전성 안내
2. 장기 탐색과 최근 소비의 분리 해석
3. 분석 가능한 범위와 한계 고지
4. 시청 리듬 시각화
5. 몰아보기 세션 분석

이 문서는 현재 코드 구조(`src/parsers.ts`, `src/features.ts`, `src/types.ts`, `src/main.ts`)를 유지하면서 기능을 단계적으로 추가하기 위한 구현 기준이다.

## 1. 유지할 원칙

- 모든 파싱과 계산은 브라우저 안에서 수행한다.
- 검색어, 영상 제목, 채널명 원문을 서버로 전송하거나 저장하지 않는다.
- `header`를 서비스 구분의 기준으로 사용한다.
- YouTube 영상, YouTube Music, 커뮤니티 게시물은 서로 다른 통계 대상으로 유지한다.
- 데이터에 없는 정보를 추정하지 않는다.
- 데이터가 부족한 경우 임의의 0이나 확정적인 문구 대신 `데이터 부족` 또는 제한 안내를 표시한다.
- 탐험형·몰입형 같은 성격 유형을 부여하지 않는다.
- YouTube Data API, LLM, 외부 API, 로컬 키워드 기반 관심사 분류는 이번 범위에서 제외한다.

## 2. 현재 구현과의 관계

현재 구현에는 다음 기능이 이미 있다.

- ZIP 업로드 및 브라우저 내 압축 해제
- 시청 기록과 검색 기록 자동 탐색
- 영상과 커뮤니티 게시물 분리
- YouTube와 YouTube Music 분리 집계
- 총 기록, 채널, 요일, 시간대, 반복 시청, 다양성, 검색어 통계
- GitHub Pages 배포

따라서 이번 작업은 파서를 새로 나누는 대규모 구조 변경보다, 기존 `RecapStats`에 필요한 계산 결과를 추가하고 결과 화면을 확장하는 방식으로 진행한다.

## 3. 1단계 — 데이터 기간과 완전성 안내

### 목표

검색 기록과 시청 기록이 서로 다른 기간을 포함할 수 있다는 점을 사용자에게 숨기지 않는다. 정교한 단일 점수보다 실제 기간과 누락 비율을 먼저 보여준다.

### 데이터 모델 변경

`src/types.ts`에 다음 타입을 추가한다.

```typescript
export interface DateCoverage {
  searchStart: Date | null;
  searchEnd: Date | null;
  watchStart: Date | null;
  watchEnd: Date | null;
  searchDays: number;
  watchDays: number;
  watchChannelCoverage: number;
  hasSearchData: boolean;
  hasSubscriptionData: boolean;
}
```

`RecapStats`에는 `coverage: DateCoverage`를 추가한다.

구독 CSV는 현재 채널명 매핑에만 사용되고 있으므로, 구독 파일이 존재했는지와 행 수를 별도로 전달할 수 있도록 `WatchData`에 다음 정보를 추가한다.

```typescript
subscriptionCount: number;
```

### 계산 방식

- 검색 기간: 검색 기록의 가장 이른 날짜부터 가장 늦은 날짜까지
- 시청 기간: YouTube 영상 기록의 가장 이른 날짜부터 가장 늦은 날짜까지
- 기간 계산은 날짜 차이를 일 단위로 계산하되, 양 끝 날짜를 포함한다.
- `watchChannelCoverage`는 `channelId`가 있는 영상 수 / 전체 YouTube 영상 수로 계산한다.
- 기록이 없거나 기간을 계산할 수 없으면 해당 값은 `0` 또는 `null`로 둔다.
- `searchCoverage` 같은 이름의 추상 점수는 실제 근거가 정해질 때까지 추가하지 않는다.

### 화면 표시

결과 화면 상단 또는 리듬 섹션 위에 다음 정보를 표시한다.

- 검색 기록 기간
- 시청 기록 기간
- 채널 정보가 확인된 시청 기록 비율
- 구독 정보 존재 여부

조건부 안내 문구 예시:

- 검색 기간이 시청 기간보다 훨씬 길 때: `검색은 장기간의 탐색 기록이고, 시청 통계는 최근 기록 중심입니다.`
- 채널 정보가 부족할 때: `일부 시청 기록에는 채널 정보가 없어 채널 관련 지표에서 제외될 수 있습니다.`
- 검색 기록이 없을 때: `검색 기록이 없어 장기 탐색 성향은 계산하지 않았습니다.`

### 완료 기준

- 검색 기록과 시청 기록의 기간이 각각 정확히 표시된다.
- 검색 기록이 없거나 시청 기록이 없는 경우 화면이 깨지지 않는다.
- 기간 불균형이 있을 때 사용자가 최근 시청 중심 해석임을 알 수 있다.
- 기존 총계와 채널 통계의 값이 변하지 않는다.

## 4. 2단계 — 장기 탐색과 최근 소비 분리

### 목표

검색 기록을 시청 기록과 단순 합산하지 않고, 서로 다른 의미의 활동으로 표현한다.

### 계산 방식

기존 통계 계산을 다음 두 그룹으로 나눈다.

- 장기 탐색: 검색 기록 수, 검색 기간, 인기 검색어, 검색 시간대
- 최근 소비: 시청 기록 수, 시청 기간, 최다 채널, 반복 시청, 활동 리듬

기존 `topSearches`는 유지한다. `RecapStats`에 검색 기간을 별도 저장하지 않고 `coverage`에서 참조하도록 하여 기간 데이터가 중복되지 않게 한다.

### 화면 구조

결과 화면의 제목이나 총계에서 검색과 시청을 같은 `전체 기록`으로 과도하게 묶지 않는다.

권장 표시 순서:

1. 최근 소비 요약: 시청 수, 활동일, 최다 채널
2. 장기 탐색 요약: 검색 수, 인기 검색어, 검색 기간
3. 두 기록의 기간 차이를 설명하는 안내

문구는 성격 판단이 아니라 관찰 가능한 사실로 작성한다.

좋은 예:

> 검색은 장기간 이어졌고, 시청 기록은 최근 기간에 집중되어 있습니다.

피해야 할 예:

> 당신은 본질적으로 탐험형입니다.

### 완료 기준

- 검색 수가 시청 수와 합쳐져 하나의 활동량처럼 오해되지 않는다.
- 검색 기록이 없을 때 장기 탐색 영역이 자연스럽게 비활성화된다.
- 기존 검색어 TOP 5 기능은 유지된다.

## 5. 3단계 — 분석 범위와 한계 고지

### 목표

결과 화면 하단에 분석에 포함되지 않은 정보와 해석상의 주의점을 영구적으로 표시한다.

### 표시할 내용

다음 항목을 고정 footer 또는 `notes` 영역에 추가한다.

- Takeout 기록에는 실제 재생 시간이 없으므로 시청 시간이나 총 재생 분량은 계산하지 않음
- Shorts 표식은 제목의 `#shorts` 문자열 기준이므로 완전한 Shorts 분류가 아님
- Live 여부와 광고 시청 여부를 확정할 수 없음
- 채널 정보가 없는 기록은 채널별 지표에서 제외될 수 있음
- 구독 정보에는 구독 시점이 없어 현재 구독 목록만 사용함
- 관심사 카테고리나 성격 유형은 계산하지 않음

### 구현 방식

- 고정 문구는 `src/main.ts`의 결과 화면에 직접 두거나, 길어질 경우 별도 상수로 분리한다.
- 숫자나 계산 결과를 포함하는 안내는 `RecapStats`를 이용해 동적으로 생성한다.
- 개인정보 원문을 footer에 출력하지 않는다.

### 완료 기준

- 결과를 새로 계산해도 한계 고지가 항상 표시된다.
- 모바일 화면에서 문장이 잘리지 않고 줄바꿈된다.
- 한계 고지가 핵심 결과보다 시각적으로 과도하게 강조되지 않는다.

## 6. 4단계 — 시청 리듬 시각화

### 목표

최다 요일과 최다 시간대만 보여주는 현재 화면을 요일 × 시간대 분포로 확장한다.

### 데이터 모델

`RecapStats`에 다음 결과를 추가한다.

```typescript
export interface RhythmCell {
  day: number;
  hour: number;
  count: number;
}

export interface DailyActivity {
  date: string;
  count: number;
}
```

```typescript
rhythm: RhythmCell[];
dailyActivity: DailyActivity[];
```

### 계산 방식

- 대상은 일반 YouTube 영상 기록으로 한정한다.
- `Date.getDay()`로 요일을 `0`부터 `6`까지 저장한다.
- `Date.getHours()`로 시간을 `0`부터 `23`까지 저장한다.
- 모든 요일·시간 조합을 미리 168개 생성해 기록이 없는 칸도 `0`으로 표현한다.
- 날짜별 활동량은 기존 통계에서 사용하는 날짜 기준과 일관되게 계산한다. 우선 기존 코드처럼 `toISOString().slice(0, 10)`을 사용하고, 시간대 문제가 발견되면 별도 결정한다.
- 일별 이동평균은 초기 구현 범위에서 제외한다. 일별 활동량이 먼저 검증된 뒤 추가한다.

### 화면 표시

초기 버전은 외부 차트 라이브러리 없이 CSS Grid로 구현한다.

- 7행 × 24열 그리드
- 셀의 색상 농도는 `count / maxCount`로 계산
- 셀에 `title` 속성으로 요일, 시간, 건수를 제공
- 색상만으로 정보를 전달하지 않도록 최고 활동 요일·시간과 평균을 텍스트로 함께 표시

함께 표시할 값:

- 평균 시청 수: 활동한 날짜 기준 하루 평균
- 최댓값: 하루 동안 가장 많이 본 기록 수
- 가장 활발한 요일
- 가장 많이 본 시간대

### 완료 기준

- 168개 셀이 항상 같은 크기로 표시된다.
- 기록이 없는 경우에도 빈 그리드 대신 `데이터 부족`이 표시된다.
- 모바일 화면에서 그리드가 화면 밖으로 깨지지 않는다.
- YouTube Music과 커뮤니티 게시물이 일반 시청 리듬에 섞이지 않는다.

## 7. 5단계 — 몰아보기 세션 분석

### 목표

연속 시청 기록을 15분 간격 기준으로 묶어 몰아보기 패턴을 관찰한다. 이 기능은 사용자의 유형을 판정하지 않고 세션 수와 규모만 보여준다.

### 세션 정의

- 일반 YouTube 영상 기록만 사용한다.
- 시간을 오름차순으로 정렬한다.
- 이전 영상과 현재 영상의 간격이 15분 이하이면 같은 세션에 포함한다.
- 간격이 15분보다 크면 새 세션을 시작한다.
- 세션은 영상 1개인 경우도 유지하되, 몰아보기 세션 수를 표시할 때는 영상 2개 이상인 세션을 별도로 집계한다.

### 데이터 모델

```typescript
export interface WatchSession {
  start: Date;
  end: Date;
  videoCount: number;
  durationMinutes: number;
}
```

`RecapStats`에는 다음 값을 추가한다.

```typescript
sessions: WatchSession[];
bingeSessionCount: number;
longestSession: WatchSession | null;
```

### 화면 표시

- 전체 세션 수
- 영상 2개 이상인 몰아보기 세션 수
- 가장 긴 세션의 영상 수와 지속 시간
- 필요한 경우 상위 세션 5개

표현 예시:

> 15분 이내 간격으로 이어진 시청을 하나의 세션으로 묶었습니다.

세션 결과를 근거로 `몰입형`, `중독형` 등의 평가 문구를 만들지 않는다.

### 완료 기준

- 시청 기록이 시간순으로 정렬되지 않아도 동일한 결과가 나온다.
- 영상이 한 개뿐인 기록도 세션 계산에서 누락되지 않는다.
- 시간 간격이 정확히 15분인 기록은 같은 세션으로 처리한다.
- 기록이 없을 때 `longestSession`은 `null`이고 화면은 오류 없이 `데이터 부족`을 표시한다.

## 8. 구현 순서와 검증

### 권장 순서

1. `types.ts`에 coverage, rhythm, session 타입 추가
2. `parsers.ts`에서 구독 행 수를 전달하도록 최소 수정
3. `features.ts`에 기간·리듬·세션 계산 추가
4. `main.ts`에 기간 안내와 한계 고지 추가
5. `style.css`에 리듬 그리드와 모바일 대응 스타일 추가
6. 실제 Takeout ZIP으로 결과 수와 화면 표시 확인

### 검증 항목

- `npm run build`
- 검색 기록이 없는 ZIP
- 구독 파일이 없는 ZIP
- 채널 정보가 없는 시청 기록만 있는 데이터
- 영상이 없는 데이터
- 15분 간격 전후의 합성 세션 데이터
- 모바일 폭에서 리듬 그리드가 가로로 넘치지 않는지 확인

### 회귀 기준

기존 기능의 다음 값은 기능 추가 후에도 변하지 않아야 한다.

- YouTube 영상 수
- YouTube Music 감상 수
- 커뮤니티 게시물 수
- Shorts 수
- 최다 채널
- 반복 시청 영상 TOP 5
- 인기 검색어 TOP 5

## 9. 이번 단계에서 하지 않는 것

- 성격·시청자 유형 판정
- 키워드 기반 관심사 분류
- 공식 YouTube 카테고리 조회
- 댓글·실시간 채팅 참여 분석
- 재생목록 생성 시점 분석
- IndexedDB 영구 캐시
- 공유 카드 이미지 생성

위 기능들은 데이터 정의와 개인정보 범위가 추가로 확정된 뒤 별도 계획으로 다룬다.