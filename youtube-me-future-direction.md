# YouTube Me 향후 방향

이 문서는 YouTube Me의 현재 UI 평가와 다음 제품 방향을 정리합니다.

## 10. UI는 나쁘지 않다. 목적이 다르다

현재 디자인은 큰 타이포, 진한 녹색, 주황 포인트, 얇은 선, 여백, 모노스페이스 보조 텍스트를 사용하는 일관된 에디토리얼·매거진 스타일이다.

따라서 핵심 문제는 디자인 완성도보다 정보 구조에 있다. 현재 결과 화면은 통계 대시보드로는 충분하지만, 사용자가 "그래서 나는 어떤 유튜브 사용자야?"라는 질문에 답을 얻기 어렵다.

UI 언어는 유지하고, 통계와 시각화 사이에 해석 레이어를 추가하는 방향으로 개선한다.

## 11. 결과 화면의 정보 구조

현재 구조:

```text
데이터 -> 통계 -> 그래프
```

개선 구조:

```text
데이터 -> 통계 -> 행동 지표 -> 해석 -> 시각화
```

예를 들어 채널 다양성, HHI, 채널 발견률, 반복 시청률, 세션 수, 활동일, 검색 빈도를 탐색성·집중성·반복성·규칙성·몰입성 점수로 변환한다. 이후 점수와 근거를 바탕으로 사용자의 시청 행동을 자연어로 설명한다.

## 12. 현재 구현 범위

- Behavior Profile 해석 레이어
- 탐색성, 집중성, 반복성, 몰입성, 규칙성의 0~100 연속형 점수
- 점수별 원시 근거 데이터
- 5개 축 오각형 레이더 차트
- 자세히 보기에서 점수별 계산 이유와 근거 표시

점수는 유형을 확정하는 라벨이 아니라 관찰 기간 안의 행동 벡터다. 데이터가 부족한 경우에는 단정적인 해석을 피하고 근거 부족 상태를 표시한다.

## 지표 정의 재검토

현재 `channelDiscoveryRate`는 시간순으로 처음 등장한 채널 수를 전체 시청 기록 수로 나눈 값입니다. `repeatViewRate`는 반복 재생된 영상의 시청 기록 수를 전체 시청 기록 수로 나눈 값입니다. `channelConcentration`은 채널별 시청 비율의 제곱합(HHI)이며, `channelDiversity`는 정규화한 엔트로피입니다.

다음 작업에서는 각 지표의 분모와 단위를 명확히 정하고, UI 이름을 계산식과 일치하도록 수정해야 합니다. 예를 들면 다음과 같습니다.

- 채널 발견률과 고유 채널 비율을 별도 지표로 구분
- 반복 시청률은 영상 단위가 아닌 시청 기록 단위로 표시
- 집중도와 다양성은 서로 반대 방향의 해석 축으로 표시

## 13. 유형화와 장기 AI/ML 방향

현재 단계에서는 MBTI처럼 4~8개 유형을 먼저 만들지 않는다. 사람을 임의의 유형에 끼워 넣는 대신, 사용자마다 행동 벡터를 만들고 충분한 표본이 확보된 뒤 실제 분포에서 군집을 발견한다.

```text
사람 -> 행동 벡터 생성
			 ↓
		 군집 발견
			 ↓
		유형 이름 부여
```

향후 검토할 모델:

- K-Means
- HDBSCAN
- Gaussian Mixture

군집화 전에는 익명화된 집계 벡터, 결측치 처리, 최소 표본 수, 군집 안정성, 결과 해석 가능성을 먼저 정의한다. 실제 데이터에서 반복적으로 관찰되는 군집만 유형으로 이름 붙인다.
