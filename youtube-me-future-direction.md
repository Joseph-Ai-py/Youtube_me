# YouTube Me 향후 방향

이 문서는 이번 변경에서 구현하지 않고 다음 기획 단계로 남겨 둔 항목을 정리합니다.

## 이번에 구현하지 않는 항목

- Behavior Profile 해석 레이어
- 탐색성, 집중성, 반복성 같은 연속형 행동 점수
- 시청자 유형을 4~8개 범주로 분류하는 기능
- K-Means, HDBSCAN 등 사용자 군집화 모델
- 지표 이름과 계산식의 전면적인 재정의

## 지표 정의 재검토

현재 `newChannelRate`는 전체 시청 횟수 중 처음 등장한 채널의 시청 횟수 비율이 아니라, 시간순으로 처음 발견한 채널 수를 전체 시청 횟수로 나눈 값입니다. `repeatedVideoRate`도 반복 등장한 영상의 시청 횟수를 전체 시청 횟수로 나눈 값입니다.

다음 작업에서는 각 지표의 분모와 단위를 명확히 정하고, UI 이름을 계산식과 일치하도록 수정해야 합니다. 예를 들면 다음과 같습니다.

- `newChannelDiscoveryRate`: 전체 고유 채널 중 처음 발견한 채널의 비율인지 정의
- `repeatViewRate`: 반복 시청에 해당하는 기록의 비율인지 정의
- 채널 수 기반 지표와 시청 횟수 기반 지표를 구분

## Behavior Profile 제안

먼저 유형명을 붙이지 않고 연속형 점수 벡터를 제공하는 방향을 권장합니다.

- 탐색성: 새 채널·새 주제·검색 다양성
- 집중성: 채널 집중도와 특정 주제 편중
- 반복성: 동일 영상 재시청 비율
- 몰입성: 세션 길이와 연속 시청 빈도
- 규칙성: 활동 요일·시간의 안정성

각 점수는 0~100 범위로 표시하고, 계산에 사용한 원시 지표와 데이터 부족 여부를 함께 보여 줍니다.

## 장기 AI/ML 방향

충분한 사용자 표본과 검증된 행동 벡터가 확보된 뒤에만 군집화를 검토합니다. 먼저 익명화된 집계 벡터, 결측치 처리, 표본 수, 군집 안정성, 결과 해석 가능성을 정의해야 합니다. 이후 K-Means와 HDBSCAN을 비교하고, 임의로 유형을 만들지 않고 실제 데이터에서 반복적으로 관찰되는 군집만 채택합니다.
