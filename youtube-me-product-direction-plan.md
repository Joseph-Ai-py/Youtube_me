# YouTube Me 제품 방향 및 다음 단계 계획

> 기존 분석 엔진은 충분히 강하다. 다음 단계의 핵심은 기능을 무작정 늘리는 것이 아니라, 이미 계산할 수 있는 데이터를 모바일에서 이해하기 쉽고 재미있게 보여주는 제품 경험으로 재구성하는 것이다.

## 1. 한 줄 결론

YouTube Me는 **Google Takeout 진입장벽을 낮추고, 기존 분석 결과를 모바일 카드형 UX로 재구성하며, 사용자가 어떤 방식으로 YouTube를 소비하는지 보여주는 서비스**로 발전한다.

장기적으로는 개인 분석 결과에서 익명화된 사용자 행동 Feature Vector를 만들고, 사용자 유형 비교와 클러스터링으로 확장한다.

핵심 방향은 다음과 같다.

```text
Takeout 온보딩
    -> 로컬 분석
    -> 핵심 결과 카드
    -> YouTube 소비 유형
    -> 상세 분석
    -> 공유/비교
    -> 익명 데이터 동의
    -> 사용자 군집화
```

## 2. 현재 상태와 문제 정의

### 이미 갖춘 것

- Google Takeout ZIP 브라우저 내 파싱
- YouTube 시청 기록 분석
- YouTube 검색 기록 분석
- 구독 데이터 분석
- 채널 다양성 및 집중도 분석
- 반복 시청 분석
- 요일 x 시간대 168셀 리듬 분석
- 세션 및 몰아보기 분석
- YouTube Music 분석
- 원본 데이터의 서버 전송 없이 로컬 처리

### 지금 해결해야 할 것

| 영역 | 상태 | 다음 조치 |
|---|---|---|
| 분석 엔진 | 충분히 구현됨 | 기존 계산을 재사용 |
| 모바일 UX | 개선 필요 | 모바일 우선 단일 카드 흐름 |
| 결과 정보량 | 너무 많음 | 핵심 5~6개와 상세 분석 분리 |
| Takeout 안내 | 부족함 | 단계형 온보딩과 프라이버시 안내 |
| 사용자 유형 | 미구현 | 설명 가능한 규칙 기반 점수 |
| 관심사 | 미구현 또는 제한적 | 제목·검색어 기반 로컬 분류부터 시작 |
| 공유/비교 | 후순위 | 카드 결과 안정화 후 추가 |
| 사용자 데이터 수집 | 미구현 | 명시적 동의 이후에만 설계 |

문제는 분석할 수 있는 데이터가 부족한 것이 아니라, 사용자가 결과를 한 번에 해석하기 어렵다는 점이다.

## 3. 제품 원칙

1. **모바일 우선**: 한 화면에 모든 통계를 넣지 않고 스크롤하며 카드 한 장씩 소비하도록 한다.
2. **핵심과 상세 분리**: 핵심 결과는 5~6개로 제한하고 나머지는 상세 분석에서 접어 보여준다.
3. **관심사와 유형 분리**: 관심사는 무엇을 보는지, 유형은 어떻게 보는지를 계산한다.
4. **행동 기반 해석**: 심리검사처럼 단정하지 않고 관찰 가능한 지표로 설명한다.
5. **로컬 우선**: 원본 기록, 검색어, 영상 제목, 채널명은 브라우저 밖으로 보내지 않는다.
6. **계산과 설명 분리**: 점수는 재현 가능한 규칙으로 계산하고, 문장은 그 결과를 이해하기 쉽게 설명한다.
7. **데이터에 없는 정보는 추정하지 않음**: Takeout 기록만으로 실제 재생 시간, 시청 완료율, 광고 시청 여부를 확정하지 않는다.
8. **불충분한 데이터는 명시**: 데이터가 부족하면 임의의 0이나 확정적인 성격 문구를 표시하지 않는다.

## 4. 목표 사용자 흐름

```text
1. YouTube Me 소개
2. Google Takeout 데이터 가져오기
3. 로컬 분석 및 개인정보 안내
4. 대표 유형 카드
5. 가장 강한 습관 카드
6. 반복 시청 카드
7. 몰입 시청 카드
8. 관심사 카드
9. 상세 분석
10. 공유 또는 비교 기능
```

## 5. Google Takeout 온보딩

첫 화면에서 데이터가 필요한 이유와 가져오는 방법을 설명한다.

### 안내 단계

1. **Google Takeout 열기**
2. **YouTube 및 YouTube Music 선택**
3. **YouTube 기록이 포함되어 있는지 확인**
4. **ZIP 파일 다운로드**
5. **YouTube Me에 ZIP 가져오기**

시각적으로는 다음 흐름을 보여준다.

```text
Google Takeout
      |
      v
[YouTube 및 YouTube Music 선택]
      |
      v
[ZIP 다운로드]
      |
      v
YouTube Me
      |
      v
[나의 YouTube 분석]
```

반드시 강조할 문구:

> 원본 데이터는 서버로 전송되지 않습니다. 이 브라우저에서만 분석됩니다.

화면에는 다음도 함께 제공한다.

- 데이터 파일 선택 버튼
- 지원 파일과 폴더 구조에 대한 간단한 안내
- 분석 가능한 기록과 한계
- 파일 처리 완료 및 누락 파일 경고

## 6. 모바일 카드형 결과 구조

핵심 결과는 분석 리포트가 아니라 카드뉴스처럼 구성한다.

### 카드 1: 나의 유형

```text
YOUR YOUTUBE STYLE

콘텐츠 탐험가
82점

새로운 채널과 콘텐츠를 찾아보는 행동이 강합니다.
```

대표 유형 하나를 먼저 보여주되, 상세 화면에서는 네 가지 유형 점수를 함께 공개한다.

### 카드 2: 가장 강한 습관

```text
YOUR RHYTHM

가장 많이 보는 시간
일요일 16시
```

### 카드 3: 같은 영상 반복

```text
YOUR REPLAY

가장 많이 다시 본 영상
영상 제목
7회
```

Exact Replay와 Topic Replay는 반드시 분리한다.

- **같은 영상 반복**: 동일한 `video_id`
- **비슷한 콘텐츠 반복**: 제목 또는 향후 임베딩 기반 유사 콘텐츠

### 카드 4: 몰입

```text
YOUR BINGE

가장 긴 세션
123개
1시간 26분
```

단, Takeout에는 정확한 재생 시간이 없으므로 시간은 실제 시청 시간이 아니라 기록 간격으로 추정한 값인지 명확히 구분해야 한다. 정확한 재생 시간이 없으면 연속 기록 수 중심으로 표시한다.

### 카드 5: 관심사

```text
YOUR INTERESTS

게임 31%
음악 24%
역사 18%
```

표현은 `시청 시간 비중`이 아니라 **기록상 소비 비중**으로 한다.

### 카드 6: 상세 분석

상세 분석은 섹션별 접기/펼치기를 사용한다.

- 시청 통계
- 채널 분석
- 검색 분석
- 시청 리듬
- 세션 분석
- YouTube Music 분석
- 데이터 기간과 분석 한계

## 7. 관심사와 소비 유형의 구분

### 관심사: 무엇을 보는가

영상 제목과 검색어를 카테고리화해 콘텐츠 주제의 분포를 계산한다.

초기 카테고리 후보:

- 게임
- 음악
- 역사
- 영화/드라마
- IT/AI
- 교육
- 스포츠
- 기타

### Type: 어떻게 보는가

채널 다양성, 신규 채널, 반복, 세션, 검색 다양성 같은 행동 지표를 조합한다.

```text
Raw Data
    |
    v
기초 Feature
    |
    +--> 콘텐츠 분석 --> 관심사
    |
    +--> 행동 분석   --> YouTube Type
```

## 8. 관심사 계산 1차 설계

초기에는 외부 API나 임베딩 모델 없이 브라우저에서 규칙 기반 분류를 사용한다.

### 분류 입력

각 시청 기록과 검색 기록에 다음 정보를 붙인다.

```text
video_id 또는 query
text
category
subcategory (선택)
confidence
```

### 규칙 기반 키워드 예시

```text
게임: 좀보이드, 마인크래프트, 롤, 배틀그라운드, 스팀, 게임
역사: 조선, 고려, 전쟁, 역사, 삼국지
음악: 노래, cover, lyrics, 가사, K-pop, 아이돌
```

채널명만으로 카테고리를 확정하지 않는다. 같은 채널 안에서도 영상 주제가 다를 수 있기 때문이다.

### 관심사 점수

관심사 카테고리 `c`의 초기 점수는 다음처럼 계산한다.

$$
InterestScore(c) = 0.60 \\times WatchScore(c) + 0.25 \\times SearchScore(c) + 0.15 \\times ReplayScore(c)
$$

- `WatchScore`: 해당 카테고리 시청 기록 비율
- `SearchScore`: 해당 카테고리 검색 기록 비율
- `ReplayScore`: 해당 카테고리 반복 시청 비율

시청 기간과 검색 기간이 다를 때는 검색 점수를 시청 기간과 겹치는 구간으로 제한한다. 전체 검색 기록은 별도로 장기 검색 관심사에 사용할 수 있다.

최종 표시 전 카테고리 점수의 합이 100%가 되도록 정규화한다.

### 향후 확장

규칙 기반 분류가 충분히 검증된 뒤 다음 순서로 확장한다.

```text
키워드 규칙
    -> 제목 기반 보강
    -> 계층형 카테고리
    -> 필요할 때만 임베딩
```

임베딩이나 외부 API는 초기 로컬 분석의 필수 조건이 아니다.

## 9. YouTube Type 1차 설계

처음부터 하나의 성격으로 고정하지 않고, 네 가지 행동 점수를 모두 계산한다. 점수는 0~100으로 표시하며 가장 높은 점수를 대표 유형으로 사용한다.

### 9.1 콘텐츠 탐험가

새로운 채널과 다양한 콘텐츠를 찾아보는 정도다.

$$
Explorer = 0.40 \\times NewChannelRate + 0.35 \\times ChannelDiversity + 0.15 \\times SearchDiversity + 0.10 \\times NewVideoRate
$$

주요 근거:

- 구독하지 않은 채널 시청 비율
- 실제 시청 분포 기반 채널 다양성
- 검색어 다양성
- 새로운 영상 비율

신규 채널 발견률은 다음처럼 정의한다.

$$
NewChannelRate = \\frac{구독하지 않은 채널의 시청 수}{채널 정보가 있는 전체 시청 수}
$$

### 9.2 취향 집중형

특정 채널이나 영상을 반복해서 보는 정도다.

$$
Focus = 0.40 \\times ChannelConcentration + 0.35 \\times ExactReplay + 0.15 \\times TopicReplay + 0.10 \\times TopChannelShare
$$

채널 집중도는 HHI 또는 그와 동등한 정규화 지표를 사용한다. 구독 채널 수는 실제 시청 분포를 대체하지 않는다.

### 9.3 몰입 시청자

한 세션 안에서 연속적으로 많은 영상을 소비하는 정도다.

$$
Immersion = 0.45 \\times BingeRatio + 0.30 \\times AverageSessionLength + 0.15 \\times MaxSessionNormalized + 0.10 \\times ConsecutiveWatchRate
$$

현재 몰아보기 정의가 2개 이상 연속 시청이라면 결과 화면에도 이 기준을 짧게 표시한다.

### 9.4 다채로운 시청자

채널뿐 아니라 주제와 검색 행동까지 여러 방향으로 분산되는 정도다.

$$
Variety = 0.50 \\times ChannelDiversity + 0.35 \\times InterestDiversity + 0.15 \\times SearchDiversity
$$

채널 다양성만으로 다채로운 시청자를 판정하지 않는다.

## 10. 정규화와 점수 표현

각 원천 지표는 횟수, 비율, 엔트로피 등 단위가 다르므로 유형 점수에 넣기 전에 0~1 범위로 정규화한다.

초기 단일 사용자 버전에서는 지표의 정의된 범위와 상한을 사용한다. 여러 사용자의 데이터가 충분히 쌓인 뒤에는 전체 사용자 분포를 이용한 percentile 방식으로 바꾼다.

```text
초기 베타:
Raw Feature -> 0~1 정규화 -> Type Score

사용자 데이터 축적 후:
전체 사용자 분포 -> Percentile -> Type Score -> Clustering
```

절대값을 단순히 더하지 않는다. 총 시청 수나 세션 수가 비율 지표를 압도하지 않도록 한다.

## 11. 채널 다양성과 반복 시청 정의

### 채널 다양성

구독 채널 수가 아니라 실제 시청 횟수 분포의 엔트로피로 계산한다.

$$
Diversity = \\frac{-\\sum_i p_i \\log_2 p_i}{\\log_2 N}
$$

- `p_i`: 채널 `i`의 시청 비율
- `N`: 시청 기록에 등장한 채널 수

채널이 하나뿐인 경우 0으로 처리한다.

### 반복 시청

결과 문구와 데이터 모델에서 다음을 구분한다.

- `exactReplayRate`: 동일 `video_id` 재시청
- `topicReplayRate`: 제목 또는 콘텐츠 유사도 기반 반복

Topic Replay는 초기 버전에서 구현하지 않아도 된다. 구현 전에는 Exact Replay와 섞어 표시하지 않는다.

## 12. 검색 기록 해석

검색 기록은 장기 탐색과 관심 변화의 중요한 자료이지만, 시청 기록과 기간이 다를 수 있다.

### 핵심 결과

- 검색 TOP 5
- 검색 기록 기간
- 최근 시청 기간과 겹치는 검색 관심사

### 상세 분석 후보

- 연도별 검색량
- 연도별 상위 검색어
- 관심사 카테고리 변화
- 장기 검색과 최근 시청의 차이

검색 기록이 6년이고 시청 기록이 최근 몇 개월이라면, 둘을 단순 합산하지 않는다. 화면에서 `장기 탐색`과 `최근 소비`를 분리해 표현한다.

## 13. 프라이버시와 데이터 수집

### 현재 단계

- 모든 파싱과 계산은 브라우저 로컬에서 수행
- 원본 ZIP, 검색어, 영상 제목, 채널명을 서버에 전송하지 않음
- 분석 결과도 기본적으로 외부 저장하지 않음
- 데이터가 없거나 불완전한 경우 제한을 표시

### 장기 단계

사용자 비교와 연구를 위해 데이터를 수집할 때는 다음을 지킨다.

1. 수집 전 명시적 동의
2. 원문이 아닌 집계 Feature Vector 우선
3. 익명 식별자 사용
4. 수집 항목과 보관 기간 공개
5. 철회 및 삭제 방법 제공
6. 관심사 원문과 영상 제목은 기본 수집 대상에서 제외

장기 Feature Vector 예시:

```json
{
  "watch": {
    "total": 11097,
    "active_days": 110,
    "replay_rate": 0.10,
    "shorts_rate": 0.046
  },
  "time": {
    "peak_hour": 16,
    "peak_weekday": "Sunday",
    "weekend_ratio": 0.36
  },
  "channel": {
    "diversity": 0.86,
    "new_channel_rate": 0.29
  },
  "session": {
    "session_count": 1006,
    "binge_ratio": 0.73,
    "max_session": 123
  },
  "interest": {
    "gaming": 0.21,
    "music": 0.28,
    "history": 0.12
  },
  "type": {
    "explorer": 0.82,
    "immersive": 0.68,
    "focused": 0.31
  }
}
```

위 수치는 구조 예시이며, 실제 사용자에게 임의로 적용하는 값이 아니다.

## 14. 구현 우선순위

### 이번 업데이트

1. 모바일 우선 레이아웃
2. 핵심 결과 카드 5~6개
3. 상세 분석 접기/펼치기
4. Google Takeout 단계형 온보딩
5. 로컬 분석 및 개인정보 안내 강화
6. 신규 채널 발견률 정의 정리
7. 실제 시청 분포 기반 채널 다양성 정의 정리
8. Exact Replay와 Topic Replay 데이터 모델 분리
9. 규칙 기반 YouTube Type 네 가지의 1차 점수 계산

### 다음 업데이트

1. 검색 트렌드와 연도별 변화
2. 제목·검색어 기반 관심사 카테고리
3. 유형 점수 근거 표시
4. 공유용 결과 카드
5. Topic Replay 보강

### 이후 업데이트

1. 익명 Feature Vector 수집 동의
2. 사용자 비교
3. 사용자 분포 기반 percentile
4. PCA/UMAP 시각화
5. K-Means 또는 HDBSCAN 클러스터링
6. 나와 비슷한 YouTube 사용자
7. 전체 사용자 중 내 위치

## 15. 완료 기준

이번 제품 방향의 1차 완료 기준은 다음과 같다.

- 모바일에서 결과 카드가 한 장씩 자연스럽게 읽힌다.
- 핵심 결과와 상세 분석의 정보 구조가 분리된다.
- Takeout을 처음 사용하는 사람도 데이터 가져오기 절차를 이해할 수 있다.
- 원본 데이터가 브라우저 밖으로 전송되지 않는다는 점이 명확하다.
- 대표 유형과 네 가지 유형 점수가 행동 지표 근거와 함께 표시된다.
- 관심사와 유형이 서로 다른 의미로 표시된다.
- 시청 시간처럼 Takeout으로 알 수 없는 값을 사실처럼 표시하지 않는다.
- 기록 기간 차이와 데이터 부족이 결과에 반영된다.

## 16. 최종 제품 정의

> 관심사는 영상과 검색 데이터를 카테고리화해 사용자가 **무엇을 소비하는지** 계산하고, Type은 채널 다양성·신규 채널·반복·세션·시간 패턴을 정규화해 사용자가 **어떻게 소비하는지** 계산한다.
>
> YouTube Me는 이 두 결과를 로컬에서 안전하게 보여주고, 장기적으로는 동의한 사용자의 집계 Feature Vector를 바탕으로 실제 YouTube 사용자 유형과 비교 경험을 제공한다.
