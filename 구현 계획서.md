# YouTube 시청 기록 JSON 불러오기 구현 계획서

## 1. 목적

Google Takeout에서 내려받은 `watch-history.json` 파일을 업로드하면,
시청 기록을 빠르게 읽고 사용자가 실제 데이터가 불러와졌는지 즉시 확인할 수 있도록 한다.

이번 단계의 범위는 **시청 기록 JSON 불러오기와 화면 표시**까지다. 검색 기록,
구독 정보, 유형 분석 기능은 JSON 로딩이 안정화된 뒤 별도 단계에서 다룬다.

## 2. 현재 상태와 문제 가설

- `streamlit/app.py`는 Streamlit 업로드 컴포넌트와 데이터프레임 표시를 담당한다.
- `streamlit/parser/watch_parser.py`에는 HTML 파서와 JSON 파서가 함께 있다.
- 표준 Google Takeout JSON의 주요 필드인 `title`, `titleUrl`, `subtitles`, `time`을 읽을 수 있다.
- JSON 업로드 경로가 존재하더라도, 실제 파일 구조가 다르거나 파싱 결과가 빈 목록이면 사용자는 불러오기가 실패했는지 알기 어렵다.
- 대용량 파일에서 업로드 이벤트마다 같은 데이터를 반복 처리하면 로딩이 느려질 수 있다.

### 확인할 가설

1. 실제로 업로드하는 JSON이 배열 형태의 표준 `watch-history.json`인지 확인한다.
2. 각 기록에서 제목, 채널, URL, 시청 시간이 정상적으로 추출되는지 확인한다.
3. 파싱 결과가 0건일 때 원인을 알려주는 메시지가 표시되는지 확인한다.
4. 동일 파일을 Streamlit이 재실행하더라도 불필요한 중복 파싱이 발생하지 않는지 확인한다.

## 3. 구현 방향

### 3.1 입력 형식

- Streamlit에서 `.json` 파일만 업로드받는다.
- 파일은 UTF-8 및 UTF-8 BOM 형식을 처리한다.
- 기본 입력은 다음과 같은 Google Takeout 배열이다.

```json
[
  {
    "header": "YouTube",
    "title": "Watched 영상 제목",
    "titleUrl": "https://www.youtube.com/watch?v=...",
    "subtitles": [{"name": "채널명", "url": "..."}],
    "time": "2026-08-28T08:15:23.000Z"
  }
]
```

- 비표준 최상위 객체가 들어오는 경우에는 지원 여부를 명확히 정하고, 지원하지 않는 구조는 구체적인 오류로 안내한다.

### 3.2 파서 정리

대상 파일: `streamlit/parser/watch_parser.py`

- JSON 로딩과 검증을 한 함수에서 일관되게 처리한다.
- 유효한 기록만 반환하고, `time`이 없는 항목이나 객체가 아닌 항목은 제외한다.
- `Watched ` 같은 Google Takeout 접두사를 제거해 화면 제목을 정리한다.
- 화면과 이후 분석에서 사용할 공통 컬럼을 고정한다.
  - `title`
  - `channel`
  - `url`
  - `timestamp`
  - `product`
- 빈 배열, 잘못된 JSON, 최상위 형식 오류를 서로 구분할 수 있는 예외 메시지를 제공한다.
- HTML 파서는 이번 작업의 범위에서 삭제하지 않고 기존 호환성을 유지한다.

### 3.3 업로드 및 화면 표시

대상 파일: `streamlit/app.py`

- 업로드된 파일을 한 번만 읽어 파서에 전달한다.
- 성공 시 총 기록 수와 함께 데이터프레임을 표시한다.
- 결과가 0건이면 성공으로 오해하지 않도록 “읽을 수 있는 시청 기록이 없다”는 안내를 표시한다.
- 실패 시 사용자가 수정할 수 있도록 JSON 형식, 필수 필드, 파일 종류를 포함한 오류를 표시한다.
- 첫 화면에서 로딩 결과를 바로 볼 수 있도록 별도의 분석 화면 이동을 요구하지 않는다.
- 파일 내용 자체를 로그나 화면에 과도하게 출력하지 않는다.

### 3.4 성능

- 파싱 전에 동일한 파일을 여러 번 `getvalue()`로 복사하거나 재파싱하지 않는다.
- 필요한 경우 파일 해시 또는 Streamlit 캐시를 사용해 동일 파일 재실행 시 파싱 결과를 재사용한다.
- 대용량 파일에서 전체 원문 미리보기, HTML 변환, 불필요한 중간 구조 생성을 하지 않는다.
- 캐시는 파일 내용이 바뀌면 자동으로 무효화되어야 한다.

## 4. 작업 순서

1. 실제 `watch-history.json` 샘플의 최상위 구조와 필드 누락 사례를 확인한다.
2. `parse_watch_history_json`의 입력 검증과 컬럼 변환을 표준 형식에 맞춰 정리한다.
3. `streamlit/app.py`에서 JSON 업로드, 성공 메시지, 빈 결과 메시지, 오류 메시지를 연결한다.
4. 동일 파일 재업로드/Streamlit 재실행 시 중복 파싱 여부를 점검하고 필요한 경우 캐시를 적용한다.
5. 샘플 데이터와 잘못된 입력으로 파서 테스트를 추가한다.
6. Streamlit을 실행해 실제 브라우저에서 기록 수와 데이터프레임이 보이는지 확인한다.
7. 사용법과 지원 파일 형식을 `README.md`에 반영한다.

## 5. 검증 계획

### 단위 검증

- 정상적인 표준 JSON 배열
- 빈 배열
- `time`이 없는 항목이 섞인 배열
- `subtitles`가 없는 항목
- UTF-8 BOM JSON
- 잘못된 JSON
- 배열이 아닌 최상위 값

### 실행 검증

- `python -m py_compile streamlit/app.py streamlit/parser/watch_parser.py`
- 작은 샘플 파일로 파서가 예상 행 수를 반환하는지 확인
- Streamlit 실행 후 JSON 업로드
- 성공 메시지, 행 수, 제목/채널/시간 컬럼 표시 확인
- 빈 결과와 잘못된 파일의 오류 안내 확인
- 실제 대용량 Takeout 파일의 초기 로딩 시간과 화면 응답 확인

## 6. 완료 조건

- JSON 파일 업로드만으로 시청 기록이 화면에 표시된다.
- 정상 기록의 제목, 채널, URL, 시청 시간이 누락 없이 보인다.
- 잘못된 JSON이나 지원하지 않는 구조를 사용자가 이해할 수 있는 메시지로 안내한다.
- 기록이 0건일 때 성공으로 잘못 표시하지 않는다.
- 동일 파일을 처리할 때 불필요한 중복 로딩이 없다.
- 기존 HTML 파서와 관련 없는 기능은 변경하지 않는다.
- README에 실행 방법과 입력 파일 조건이 문서화되어 있다.

## 7. 제외 범위

- 시청 기록의 통계 차트와 유형 분류
- 검색 기록 및 구독 정보 JSON 지원
- YouTube API 연동
- 데이터베이스 저장
- 원본 JSON 수정 또는 삭제
