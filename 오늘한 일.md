# 📅 2026-08-25 오늘한 일

## 🎯 오늘의 목표

Google Takeout에서 가져온 YouTube ZIP 데이터를
우리 서비스에서 읽을 수 있는지 확인하고,
분석에 사용할 핵심 데이터 파일을 자동으로 찾는 것.

---

# 1. 프로젝트 환경 구축

## 사용 기술

- React
- TypeScript
- Vite
- JSZip
- PapaParse
- GitHub Codespaces

React + TypeScript 기반의 Vite 프로젝트를 생성했다.

---

# 2. ZIP 데이터 Import 구현

Google Takeout에서 내려받은 ZIP 파일을
브라우저에서 직접 읽을 수 있도록 구현했다.

사용자가:

```text
Google Takeout
      ↓
YouTube 데이터 다운로드
      ↓
ZIP 파일
      ↓
YouTube Me에 업로드
```

# 📅 2026-08-26 오늘한 일

## 🎯 오늘의 목표

기존 React + TypeScript + Vite 기반의 프로젝트를
Python + Streamlit 기반으로 전환하고,

Google Takeout에서 가져온 YouTube ZIP 파일을
Python에서 읽고 분석할 수 있는 기본 구조를 만든다.

---

# 1. 프로젝트 기술 스택 변경

기존:

```text
React
TypeScript
Vite
JSZip
PapaParse
```

# 📅 2026-09-03 오늘한 일

## 🎯 오늘의 목표

Google Takeout ZIP 하나만 업로드하면 시청 기록과 검색 기록을 자동으로 찾아
YouTube Recap을 보여주는 GitHub Pages용 브라우저 앱을 구현한다.

## 1. GitHub Pages 구조로 전환

- Vite + TypeScript 기반 정적 웹 앱 구조를 추가했다.
- `vite.config.ts`에 `/Youtube_me/` base 경로를 설정했다.
- Streamlit, Python 서버, AI, YouTube Data API, 외부 API를 사용하지 않는 구조로 확정했다.

## 2. ZIP 자동 분석 구현

- JSZip을 추가했다.
- 사용자가 ZIP 파일 하나만 선택하도록 업로드 화면을 변경했다.
- ZIP 내부에서 다음 파일을 자동으로 찾는다.

```text
시청 기록/시청 기록.json
시청 기록/검색 기록.json
구독정보/구독정보.csv
```

- 시청 기록과 검색 기록을 각각 파싱한다.
- 구독 CSV의 채널 ID와 채널명을 연결해 `UC...` 형태의 ID가 화면에 노출되지 않게 했다.

## 3. YouTube Recap 통계 구현

- 가장 많이 본 채널
- 가장 활발했던 요일과 시간대
- 활동한 날짜 수와 하루 평균 시청 수
- 주말 시청 비율
- 반복 시청 영상 TOP 5
- 새 채널 발견 비율
- 채널 다양성
- 최근 시청 영상
- 인기 검색어 TOP 5

## 4. YouTube와 YouTube Music 분리

- 일반 시청 통계에는 `YouTube` 기록만 포함한다.
- `YouTube Music` 기록은 별도의 음악 감상 통계로 분리했다.
- 가장 많이 들은 음악, 감상 횟수, 아티스트 또는 채널명을 표시한다.
- 두 서비스의 사용량을 비교할 수 있도록 화면에 함께 표시한다.

## 5. 문서화와 검증

- README를 현재 ZIP 업로드 및 GitHub Pages 사용법에 맞게 수정했다.
- 실제 Takeout ZIP 내부의 시청·검색·구독 파일 경로를 확인했다.
- `npm run build` 성공
- TypeScript 오류 없음
- `git diff --check` 통과